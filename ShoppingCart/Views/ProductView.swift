//
//  ProductView.swift
//  ShoppingCart
//
//  Created by Miriam Moe on 23/10/2022.
//

import SwiftUI

// Dette skal inn i CartView og denne filen slettes? 

/* struct ProductView: View {
     
     let salg:  Bool
     let valgt: Bool
     
     var body: some View {
         GeometryReader{ GeometryProxy in
             if (salg) {
                 Discounts()
                     .zIndex(1)
             }
             HStack {
                 Image(systemName: "globe")
                     .resizable()
                     .frame(width: 44, height: 44)
                     .padding(.trailing)
             }
         }
     }
 }



 struct Discounts: View {
     var body: some View {
         ZStack{
             Path {path in let length = 40.0
                 path.addLines([CGPoint(x: 0, y:0),
                                CGPoint(x: length, y:0),
                                CGPoint(x: 0, y: length)])  }
         }
         
         .foregroundColor(.orange)
         GeometryReader { geoProxy in
             let x = geoProxy.size.width/50
             Text("Salg")
                 .font(.system(size: x))
                 .fontWeight(.bold)
                 .padding(.top)
                 .rotationEffect(Angle(degrees: 312))
                 .offset(x: -x/3, y: -x/2)
         }
     }
     

 }

 
 
struct ProductView_Previews: PreviewProvider {
    static var previews: some View {
        ProductView()
    }
}
*/
